function projects(input) {
    console.log(`The architect ${input[0]} will need ${Number(input[1]) * 3} hours to complete ${input[1]} project/s.`)
}

projects(["George", "4"])
projects(["Sanya", "9"])